name: Enable GitHub Pages

# This workflow helps configure GitHub Pages
# Run this once to set up Pages deployment

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  setup-info:
    runs-on: ubuntu-latest
    steps:
      - name: Instructions
        run: |
          echo "========================================"
          echo "GitHub Pages Setup Instructions"
          echo "========================================"
          echo ""
          echo "Please follow these steps:"
          echo ""
          echo "1. Go to Settings → Pages"
          echo "2. Under 'Build and deployment':"
          echo "   - Source: Select 'GitHub Actions'"
          echo "3. Click Save"
          echo ""
          echo "Your site will be available at:"
          echo "https://andrewnakas.github.io/nakas_snow_ensamble/"
          echo ""
          echo "========================================"
